<UserControl x:Class="WAIUA.Controls.InventoryEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="Self"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Border Background="#181E34" CornerRadius="20" Margin="10" Padding="15">
		<Border.Effect>
			<DropShadowEffect BlurRadius="5" ShadowDepth="3" />
		</Border.Effect>
		<Image Source="{Binding Image, ElementName=Self}"
		       ToolTip="{Binding TooltipName, ElementName=Self}"
		       ToolTipService.InitialShowDelay="0">
			<Image.Effect>
				<DropShadowEffect BlurRadius="5" ShadowDepth="3" />
			</Image.Effect>
		</Image>
	</Border>
</UserControl>